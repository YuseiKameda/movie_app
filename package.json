{
    "name": "movie_app",
    "version": "1.0.0",
    "scripts": {
      "start": "node server/server.js",
      "heroku-postbuild": "cd client && npm install && npm run build && ../server && npm install"
    },
    "dependencies": {},
    "engines": {
    "node": "16.x",
    "npm": "8.x"
  }
  }
